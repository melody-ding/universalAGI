version: '3.8'

services:
  backend:
    command: ["python", "-m", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    environment:
      - PYTHONUNBUFFERED=1
    
  frontend:
    command: ["npm", "run", "dev"]
    stdin_open: true
    tty: true